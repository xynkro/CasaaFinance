name: WSR Full — Sunday Monday Strategy Review

on:
  schedule:
    # 11:37 UTC Sunday = 19:37 SGT Sun (for Mon morning)
    - cron: "37 11 * * 0"
  workflow_dispatch:
    inputs:
      dry:
        type: boolean
        default: false
      skip_web:
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: wsr-full
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OAUTH_TOKEN_JSON:  ${{ secrets.OAUTH_TOKEN_JSON }}
      SHEET_ID:          ${{ secrets.SHEET_ID }}
      DRIVE_FOLDER_ID:   ${{ secrets.DRIVE_FOLDER_ID }}
      MODEL_OPUS:        ${{ vars.MODEL_OPUS   || 'claude-opus-4-7' }}
      MODEL_SONNET:      ${{ vars.MODEL_SONNET || 'claude-sonnet-4-6' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Generate WSR Full
        run: |
          ARGS=""
          if [ "${{ inputs.dry }}" = "true" ]; then ARGS="$ARGS --dry"; fi
          if [ "${{ inputs.skip_web }}" = "true" ]; then ARGS="$ARGS --skip-web"; fi
          python scripts/generate_wsr_full.py $ARGS
