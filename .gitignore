# Secrets
.env
.config/
.config/client_secret*
*.bak
.state/authorized_user.json
.state/token*.json
.state/sync.log

# Local agent/tooling state (never commit)
.claude-flow/
.claude/launch.json
pwa/.claude/launch.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# PWA build artefacts
pwa/node_modules/
pwa/dist/
pwa/.vite/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Sensitive data + local DBs / tooling state — never commit (2026-06-06 security audit)
PortfolioGrabs/
*.db
*.rvf
*.rvf.lock
# Office temp lock files (e.g. from generating the mindmap deck)
~$*
# stray large media in repo root
51132802981_e748ed1ebd_o.jpg
